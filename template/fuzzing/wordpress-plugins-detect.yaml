info:
  id: wordpress-plugins-detect
  name: WordPress Plugins Detection
  author: Hung, Dat, Hoa, Danh
  severity: info
  tags: fuzz,wordpress

requests:
  - request:
      - |
        GET /wp-content/plugins/{{pluginSlug}}/readme.txt HTTP/1.1
        Host: {{Hostname}}

    payloads:
      pluginSlug: C:\Users\Admin\Desktop\Injection-Tool\config\wp-plugins.txt
    scanMode: batteringram
    threads: 50
    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200

      - type: word
        word:
          - "== Description =="
